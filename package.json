{
  "name": "@inkdpixels/commit-analyzer",
  "version": "0.0.4",
  "description": "A commit-analyzer plugin for semantic-release which uses the commit guidelines of TYPO3 CMS.",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src -d dist",
    "lint": "xo src/**/*.js",
    "mocha": "find ./src -name '*.spec.js' | xargs istanbul cover -x *.spec.js _mocha -- -R spec --compilers js:babel-register",
    "test": "npm run lint && npm run mocha && npm run build && node dist/index.js"
  },
  "author": "Tyll Weiß <inkdpixels@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "chai": "^3.4.1",
    "istanbul": "^0.4.1",
    "mocha": "^2.3.4",
    "xo": "^0.12.1"
  },
  "xo": {
    "esnext": true,
    "envs": [
      "node",
      "mocha",
      "es6"
    ]
  }
}
